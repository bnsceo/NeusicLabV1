# Cyvora personas

This directory vendors the minimal persona prompts the worker loop can resolve.

The worker requires exact filename matches for assigned agents. If you add a new
agent name in the app, add the matching `kebab-case.md` file here too.

