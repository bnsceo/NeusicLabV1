name: Neusic Product Reliability

on:
  push:
    branches:
      - main
      - agent/wave-loom-reliability-phase
      - agent/unify-neusic-product-journey
      - agent/restore-lab-mobile-agent-previews
  pull_request:
    paths:
      - 'index.html'
      - 'site-polish.*'
      - 'landing-experience.*'
      - 'neusic-suite.*'
      - 'neusic-agent.*'
      - 'agents/**'
      - 'social/**'
      - 'live-loop/**'
      - 'wave-loom/**'
      - 'app/**'
      - 'tests/wave-loom/**'
      - 'tests/neusic-suite/**'
      - 'package.json'
      - '.github/workflows/wave-loom-reliability.yml'
      - '.github/workflows/deploy-neusic-pages.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  reliability:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install test dependencies
        run: npm install --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Validate Wave reliability source
        run: npm run test:wave-core

      - name: Validate shared product source
        run: npm run test:suite-core

      - name: Validate restoration source
        run: npm run test:restoration

      - name: Run browser and audio smoke tests
        run: npm run test:browser

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: neusic-playwright-report
          path: tests/wave-loom/playwright-report
          if-no-files-found: ignore
          retention-days: 7
