{
  "name": "neusic-lab-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:wave": "node --test tests/wave-loom/source-integrity.test.mjs",
    "test:browser": "playwright test --config tests/wave-loom/playwright.config.mjs",
    "test": "npm run test:wave && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}