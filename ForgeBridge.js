const DB_NAME='neusic-forge-bridge';
const STORE='transfers';
function openDb(){return new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,1);request.onupgradeneeded=()=>{if(!request.result.objectStoreNames.contains(STORE))request.result.createObjectStore(STORE,{keyPath:'id'});};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error);});}
function encodeWav(buffer){const channels=buffer.numberOfChannels,sampleRate=buffer.sampleRate,frames=buffer.length,blockAlign=channels*2,array=new ArrayBuffer(44+frames*blockAlign),view=new DataView(array);const write=(offset,text)=>{for(let i=0;i<text.length;i++)view.setUint8(offset+i,text.charCodeAt(i));};write(0,'RIFF');view.setUint32(4,36+frames*blockAlign,true);write(8,'WAVE');write(12,'fmt ');view.setUint32(16,16,true);view.setUint16(20,1,true);view.setUint16(22,channels,true);view.setUint32(24,sampleRate,true);view.setUint32(28,sampleRate*blockAlign,true);view.setUint16(32,blockAlign,true);view.setUint16(34,16,true);write(36,'data');view.setUint32(40,frames*blockAlign,true);let offset=44;for(let frame=0;frame<frames;frame++){for(let channel=0;channel<channels;channel++){const sample=Math.max(-1,Math.min(1,buffer.getChannelData(channel)[frame]||0));view.setInt16(offset,sample<0?sample*0x8000:sample*0x7fff,true);offset+=2;}}return array;}
export async function storeForForge(buffer,name,source='Live Loop Lab'){const id=`forge-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`,blob=new Blob([encodeWav(buffer)],{type:'audio/wav'}),record={id,name:name||'Live Loop Transfer',source,mime:'audio/wav',duration:buffer.duration,createdAt:Date.now(),blob};const db=await openDb();await new Promise((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});db.close();return id;}
export async function sendToForge(buffer,name){const popup=window.open('about:blank','_blank');const id=await storeForForge(buffer,name);const url=new URL('../wave-loom/',location.href);url.searchParams.set('forgeTransfer',id);url.searchParams.set('source','live-loop');if(popup)popup.location.href=url.href;else location.href=url.href;return id;}
export function downloadBuffer(buffer,name='neusic-live-loop.wav'){const blob=new Blob([encodeWav(buffer)],{type:'audio/wav'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=name;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),4000);}
